function files = rdir(pattern)
%% function files = rdir(pattern)
% Finds all files matching 'pattern' recursively
% dir will be called in each directory



end


function [files, folders] = rdirff(files, folders)


end